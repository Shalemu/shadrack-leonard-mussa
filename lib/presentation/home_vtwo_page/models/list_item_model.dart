class ListItemModel {
String title,img,day, month, price, location;
  ListItemModel({

    required this.title,required this.img,required this.location,required this.price,required this.day,required this.month
  });

 }
